# Where the JuanWise API lives. Defaults to the deployed Vercel instance when
# unset, so a fresh clone works with no setup.
#
# Point it at a laptop running `npm run dev` in juanwise-be with:
# VITE_API_URL=http://localhost:3000
VITE_API_URL=https://juanwise-be.vercel.app
